
---
const { src, body } = Astro.props
---
<li class="mpp-feature-item">
    <img src={src} />
    <div class="mpp-feature-body" set:html={body} />
</li>

<style scoped lang="scss">
    li {
        background: #fff;
        border-radius: 15px;
        overflow: hidden;
        box-shadow: 0 4px 16px 0 rgba(0, 0, 0, 0.2);
        outline: 1px solid rgba(0,0,0,0);
        transition: ease .5s;
        @media (max-width: 992px) {
            min-width: 210px;
        }
        @media (min-width: 992px) {
            flex: 1;
        }
        &:hover {
            outline: 1px solid #F8714C;
        }
    }
    img {
        object-fit: cover;
        height: 175px;
        width: 100%;
    }
    .mpp-feature-body {
        padding: 0.45em 1em 1em;
    }
</style>

<style lang="scss" >
    .mpp-feature-body {
        & > h3 {
            font-family: "Montserrat";
            font-size: .875rem;
            font-weight: 600;
        }
        & > p {
            font-size: .625rem;
            line-height: 1.4;
            margin: 0.6em 0 0 0;
        }
    }
</style>